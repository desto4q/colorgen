import { style as tw } from "twrnc";

interface IColors {
	color: string;
	label: string;
}
interface IMono {
	shades: string[];
	tints: string[];
}

let snapper=() => {
	
}
export { tw };

export type { IColors, IMono };
